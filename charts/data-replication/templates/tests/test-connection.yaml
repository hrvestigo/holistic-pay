apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "data-replication-ms.name" . }}-test-connection"
  labels:
    {{- include "data-replication-ms.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: curl
      image: alpine
      command:
        - 'bash'
        - -c
        - 'apk add curl && curl -k https://{{ include "data-replication-ms.name" . }}:{{ .Values.service.port }}/health/liveness'
  restartPolicy: Never
