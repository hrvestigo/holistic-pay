apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "consumer-finance-web.name" . }}-test-connection"
  labels:
    {{- include "consumer-finance-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: curl
      image: alpine
      command:
        - 'bash'
        - -c
        - 'apk add curl && curl -k https://{{ include "consumer-finance-web.name" . }}:{{ .Values.service.port }}/health/liveness'
  restartPolicy: Never
